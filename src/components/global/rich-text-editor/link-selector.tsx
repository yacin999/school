import React from 'react'

type Props = {
    open : boolean,
    onOpenChange : (open : boolean) => void
}

const LinkSelector = (props: Props) => {
  return (
    <div>LinkSelector</div>
  )
}

export default LinkSelector